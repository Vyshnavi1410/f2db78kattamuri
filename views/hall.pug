extends layout

block content
  h1= title
  - let result  = results;
  div(class="container-fluid")
  div(class="row")
   each value,index in results
    div(class="col-sm-2" onclick=`buy(${index})`)  #{value.hall_name}, #{value.hall_rent}, #{value.hall_size}
   script.
    function buy(index)
    {
      alert(`Your purchased Item ${index+1}`)
    }